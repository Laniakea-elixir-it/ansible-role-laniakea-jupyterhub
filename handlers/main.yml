---
# handlers file for jupyterhub

- name: Restart jupyterhub
  systemd:
    name: "{{ jupyterhub_unit_name }}"
    state: restarted
    enabled: yes
    daemon_reload: yes

- name: Restart nginx
  service:
    name: nginx
    state: restarted